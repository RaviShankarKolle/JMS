Borrow Service Functionalities
Create borrow record for a user and book with start/end dates.
Validate borrow rules (for example: user eligibility/status and borrowing constraints).
Retrieve borrow details/history (including user-specific history with pagination).
Update borrow lifecycle states (active, returned, overdue handling).
Trigger overdue processing through scheduled jobs.
Integrate with notification service for borrow confirmation and reminder-related flows.
Expose REST APIs through gateway route (/api/v1/borrows/**).
Provide Swagger/OpenAPI docs (/swagger-ui.html, /v3/api-docs and gateway docs path).
Include JUnit/Mockito unit tests for service/business logic and major scenarios.


Fine-Payment Service Functionalities
Calculate and manage fines for overdue/lost-book scenarios.
Create and track fine records per user/borrow context.
Process fine payments and update fine status after payment.
Provide endpoints for outstanding fines and payment history.
Support scheduled/triggered reminder-related fine workflows.
Integrate directly with notification service for fine reminder/payment update notifications.
Expose REST APIs through gateway route (/api/v1/fines/**).
Provide Swagger/OpenAPI docs (/swagger-ui.html, /v3/api-docs and gateway docs path).
Include JUnit/Mockito unit tests for fine calculation and payment business flows.
Notification Service Functionalities
Send registration notifications.
Send borrow confirmation and overdue notifications.
Send fine reminder/payment-related notifications.
Provide REST endpoints for notification requests from other services.
Run scheduled reminder processing for pending notifications.
SMTP-based email sending integration.
Expose REST APIs through gateway route (/api/v1/notifications/**).
Provide Swagger/OpenAPI docs (/swagger-ui.html, /v3/api-docs and gateway docs path).
Include JUnit/Mockito unit tests for notification dispatch and scheduler scenarios.
